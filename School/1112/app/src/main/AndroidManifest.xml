<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a0912">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".money3_showedit"></activity>
        <activity android:name=".money3_edit" />
        <activity android:name=".Irr" />
        <activity android:name=".money2_showedit" />
        <activity android:name=".GSG" />
        <activity android:name=".money2_edit" />
        <activity android:name=".one" />
        <activity android:name=".money_showedit" />
        <activity android:name=".money_edit" />
        <activity
            android:name=".welcome"
            android:theme="@style/AppTheme.notitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <service android:name=".MyService" ></service>
        <receiver android:name=".AutoUpdateReceiver" > </receiver>
    </application>

</manifest>